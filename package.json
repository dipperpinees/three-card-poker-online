{
    "name": "three-card-poker-online",
    "version": "1.0.0",
    "repository": "git@github.com:dipperpinees/three-card-poker-online.git",
    "author": "hiepnguyen223 <hiepnguyenn223@gmail.com>",
    "license": "MIT",
    "scripts": {
        "postinstall": "node ./bin/install.js",
        "format": "prettier '**/*.{js,ts,mjs,cjs,json,css,scss,jsx,tsx}' --write",
        "dev:api": "npm run dev --prefix api",
        "dev:web": "npm run start --prefix web",
        "build:web": "npm run build --prefix web",
        "build:api": "npm run build --prefix api",
        "build": "npm run build:web && npm run build:api",
        "start:prod": "npm run start --prefix api"
    },
    "devDependencies": {
        "prettier": "^3.0.3"
    }
}
